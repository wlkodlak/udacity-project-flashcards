import React from "react"
import renderer, { ReactTestRendererJSON } from "react-test-renderer"

import App from "./App"

describe("Application", () => {
    it("initially shows decks list", () => {
        fail("I have no idea how to write such test")
    })
})
